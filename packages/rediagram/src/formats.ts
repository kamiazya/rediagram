import { createImageProcessor } from './image-processor';

export const PNG = createImageProcessor('png');
export const SVG = createImageProcessor('svg');
export const PDF = createImageProcessor('pdf');
